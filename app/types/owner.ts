export interface Owner {
    id: number;  // Assuming you have an ID field for each owner
    name: string;
    contact_info: string;  // Email field as string
    address: string;  // Optional, can be blank
  }
  